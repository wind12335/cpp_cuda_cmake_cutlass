# 修改对照表（2026-09-01）

> **第三轮修改记录（2026-09-02，对照论文 eurosys27-fall-anonymous.pdf §3-§4 后）**：
> 1. 权1步骤2："在其所需的数据生成或到达后"→"在其所需的数据具备后"（消除残留"或"），并重组为"……即发布该局部工作单元的细粒度可消费就绪状态，以使各局部工作单元可被消费侧独立消费"；
> 2. 权6：按论文 frontier-first 的 wake-up/tail 划分改写为"前沿工作单元/尾段工作单元"表述，与权10/12"前沿优先、尾段跟进"形成术语链；
> 3. 权10/12："最早能够触发"→"最早可触发"（对齐原稿用词）；
> 4. 说明书按论文补三处机制细节：实施例1步骤2 末增设备侧行块屏障等待句；实施例2/发明内容优选段增 arrival/free 单调递增票据机制；实施例3步骤3 增条带票据配对复用句；
> 5. 文风自查修正13处：清理论文腔用词（下沉/错配/供给/给出），"能够"回归"可"字系，修复"等待消费数据"连排；摘要改为"以关键路径优先方式执行通信与计算重叠"（299字）；
> 6. **已定决策**：实施例3 暂存公式 Q=W*B*P_local*R 维持原稿不改；不新增 AG 回退机制权利要求；不补"实施例效果验证"节。
> - 下文引用的"修改稿"文本为第一轮状态，实际文档以修改稿文件夹中的 docx 为准；批注对照与段落去向说明仍有效。
> **⚠ 第二轮修改记录（2026-09-02）**：应用户要求，已将三个英文算子名全部替换为中文名称，不再使用英文加括号注释的形式：
> - AllGather-GEMM（全收集-通用矩阵乘）→ **全收集-通用矩阵乘**
> - GEMM-ReduceScatter（通用矩阵乘-归约分散）→ **通用矩阵乘-归约分散**
> - GEMM-AllReduce（通用矩阵乘-全归约）→ **通用矩阵乘-全归约**
> - 变体 "AllGather路径" → "全收集路径"；共替换 77 处（权利要求书18、说明书56、摘要3），名称两侧残留的半角空格已清理。
> - 摘要同步重写（294字），并恢复了"行块工作单元、面板工作单元"的具体提法。
> - **注意**：下文引用的"修改稿"文本为第一轮状态（仍含英文名），实际文档以修改稿文件夹中的 docx 为准；本文其余结构说明（批注对照、段落去向）仍然有效。
> - **附图提醒**：说明书附图（图1~图5）是图片，图内若绘有英文算子名无法通过文本替换修改，如需图内也中文化需重新导出图片。

- **原稿（老师批注版，未动）**：`（李奕辰-初稿-20260720）-…_反馈` 文件夹
- **修改稿**：`（李奕辰-修改稿-20260901）-…` 文件夹
- 说明：⟨…⟩ 内为从文档中抽取的数学公式内容；两版公式对象均嵌在 Word 原生位置。

---
# 一、权利要求书（逐项对照）

## 权利要求1
> 涉及批注：批注0：如何理解生产侧、消费侧？/批注1：不要用"或"、如何得到、如何定义/批注2：就绪状态具体如何建立/批注3：逻辑不通顺、推进顺序

**原稿：**

> 1、一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，包括如下步骤：步骤1、获取待执行的分布式矩阵乘相关算子的类型、矩阵问题形状、并行规模及硬件资源约束，建立生产侧与消费侧依赖模型，并确定目标执行配置参数；步骤2、根据生产侧与消费侧依赖模型，将输入数据分片或局部部分结果划分为多个局部工作单元，为各局部工作单元建立细粒度可消费就绪状态，并采用关键路径优先的推进方式组织计算与通信推进顺序；步骤3、仅为当前活动窗口内的局部工作单元分配有限数量、可循环复用的暂存窗口槽位，并依据所述目标执行配置参数执行局部工作单元的通信计算重叠；步骤4、在目标局部工作单元满足消费依赖后，触发对所述目标局部工作单元的矩阵乘消费、局部归约或全局归约，输出结果并释放对应暂存窗口槽位。

**修改稿：**

> 1、一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，包括如下步骤：步骤1、获取待执行的分布式矩阵乘相关算子的类型、矩阵问题形状、并行规模及硬件资源约束，建立生产侧与消费侧之间的依赖模型，并确定目标执行配置参数；其中，所述生产侧为提供数据的一方，包括在并行节点间传输数据的通信过程和生成局部部分结果的矩阵乘计算过程，所述消费侧为使用所述数据的一方，包括依赖所述数据执行的矩阵乘消费过程和归约过程；步骤2、根据所述依赖模型，将生产侧提供的待消费数据划分为多个能够独立判定的局部工作单元，针对每个局部工作单元，在其所需的数据生成或到达后，即建立该局部工作单元的细粒度可消费就绪状态；并采用关键路径优先的推进方式确定通信与计算的推进顺序，优先推进最早能够被消费的局部工作单元；步骤3、仅为当前活动窗口内的局部工作单元分配有限数量、可循环复用的暂存窗口槽位，所述活动窗口为当前正在执行通信与计算重叠的局部工作单元的集合，并依据所述目标执行配置参数执行局部工作单元的通信计算重叠；步骤4、在目标局部工作单元满足消费依赖后，触发对所述目标局部工作单元的矩阵乘消费、局部归约或全局归约，输出结果并释放对应暂存窗口槽位。

## 权利要求2
> 涉及批注：批注4：能翻译成中文吗？/批注1：输入数据、局部部分如何定义

**原稿：**

> 2、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，所述分布式矩阵乘相关算子包括AllGather-GEMM、GEMM-ReduceScatter和GEMM-AllReduce；其中，AllGather-GEMM的消费依赖为输入分片聚合后再执行矩阵乘消费；GEMM-ReduceScatter的消费依赖为各源节点的局部部分结果到齐后执行局部归约并保留目标输出段；GEMM-AllReduce的消费依赖为各源节点的局部部分结果到齐后在各并行节点上执行全局归约。

**修改稿：**

> 2、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，所述分布式矩阵乘相关算子包括AllGather-GEMM（全收集-通用矩阵乘）、GEMM-ReduceScatter（通用矩阵乘-归约分散）和GEMM-AllReduce（通用矩阵乘-全归约）；其中，所述生产侧提供的待消费数据在AllGather-GEMM场景下为待聚合的输入数据分片，在GEMM-ReduceScatter和GEMM-AllReduce场景下为各并行节点对本地矩阵分块执行矩阵乘得到的局部部分结果；AllGather-GEMM的消费依赖为输入分片聚合后再执行矩阵乘消费，GEMM-ReduceScatter的消费依赖为各源节点的局部部分结果到齐后执行局部归约并保留目标输出段，GEMM-AllReduce的消费依赖为各源节点的局部部分结果到齐后在各并行节点上执行全局归约。

## 权利要求3
> 涉及批注：无批注，未改写

**原稿：**

> 3、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，所述目标执行配置参数包括：局部工作单元切分粒度参数、前沿推进深度参数、活动窗口大小参数、暂存窗口槽位数量参数、通信并发度参数、计算资源配额参数及与列向带状划分相关的组织参数；所述目标执行配置参数根据待处理矩阵形状、并行规模及硬件资源约束确定。

**修改稿：**

> 3、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，所述目标执行配置参数包括：局部工作单元切分粒度参数、前沿推进深度参数、活动窗口大小参数、暂存窗口槽位数量参数、通信并发度参数、计算资源配额参数及与列向带状划分相关的组织参数；所述目标执行配置参数根据待处理矩阵形状、并行规模及硬件资源约束确定。

## 权利要求4
> 涉及批注：无批注，未改写

**原稿：**

> 4、根据权利要求3所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，确定所述目标执行配置参数的过程包括：根据待处理矩阵形状、并行规模及硬件环境生成候选配置集合；基于结构一致性约束、资源包络约束及执行可行性约束对所述候选配置集合进行过滤，得到可行配置域；对局部工作单元切分形状、前沿推进深度、活动窗口大小及暂存窗口槽位布局进行结构筛选，再对通信并发度和计算资源配额进行资源精修，以确定目标执行配置参数。

**修改稿：**

> 4、根据权利要求3所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤1中，确定所述目标执行配置参数的过程包括：根据待处理矩阵形状、并行规模及硬件环境生成候选配置集合；基于结构一致性约束、资源包络约束及执行可行性约束对所述候选配置集合进行过滤，得到可行配置域；对局部工作单元切分形状、前沿推进深度、活动窗口大小及暂存窗口槽位布局进行结构筛选，再对通信并发度和计算资源配额进行资源精修，以确定目标执行配置参数。

## 权利要求5
> 涉及批注：无直接批注，术语统一（行维→行方向）并衔接权1

**原稿：**

> 5、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在AllGather-GEMM场景下，将每个远端输入分片沿行维划分为多个行块工作单元，并在各行块工作单元完成复制后分别发布细粒度可消费就绪状态，以使消费侧在其所依赖的行块工作单元到达后触发对应输出块的矩阵乘计算。

**修改稿：**

> 5、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述局部工作单元在AllGather-GEMM场景下按照如下方式划分并建立就绪状态：将每个远端输入分片沿行方向划分为多个行块工作单元，在一个行块工作单元对应的远端数据复制完成后，即发布该行块工作单元的细粒度可消费就绪状态，以使消费侧在其所依赖的行块工作单元到达后即触发对应输出块的矩阵乘计算，而无需等待整个远端输入分片全部到达。

## 权利要求6
> 涉及批注：原文以"？？？？"结尾占位未写/批注5：逻辑不通顺、具体怎么算、推进顺序

**原稿：**

> 6、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，所述关键路径优先的推进方式组织计算与通信推进顺序的方法为：？？？？

**修改稿：**

> 6、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述关键路径优先的推进方式为：根据各局部工作单元的就绪发布情况和消费依赖关系，将最早能够被消费的局部工作单元确定为位于关键路径上的工作单元；通信侧优先传输所述关键路径上的局部工作单元，计算侧在关键路径上的局部工作单元就绪后立即执行对应的消费，使通信过程与计算过程沿关键路径衔接，其余局部工作单元的传输和计算穿插在关键路径的等待时间内执行。

## 权利要求7
> 涉及批注：批注6：对哪个特征的进一步限定/批注7：计算组织≠组织计算

**原稿：**

> 7、根据权利要求5所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在AllGather-GEMM场景下，所述关键路径优先的分岔调度方式包括：将仅依赖本地输入分片的计算组织为本地快速路径；将依赖远端输入分片的计算组织为远端消费路径；其中，本地快速路径在不等待远端输入分片到达的情况下先行执行，远端消费路径依据所述细粒度可消费就绪状态触发执行，且两条路径对应的输出写回区域互不重叠。

**修改稿：**

> 7、根据权利要求6所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述关键路径优先的推进方式在AllGather-GEMM场景下采用本地快速路径与远端消费路径的分岔调度实现：将仅依赖本地输入分片的计算划入本地快速路径，将依赖远端输入分片的计算划入远端消费路径；本地快速路径不等待远端输入分片到达即先行执行，远端消费路径依据所述细粒度可消费就绪状态逐块触发执行，且本地快速路径与远端消费路径的输出写回区域互不重叠。

## 权利要求8
> 涉及批注：无直接批注，跟随权7引用链

**原稿：**

> 8、根据权利要求7所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在AllGather-GEMM场景下，远端输入分片按照行块优先顺序进行拉取，并将局部复制屏障并入通信驱动流水线，使远端输入分片的拉取、细粒度可消费就绪状态发布以及远端消费路径的触发按统一时序推进；同时，为数据复制路径分配独立的计算资源配额，以降低数据复制路径与本地快速路径之间的资源争用。

**修改稿：**

> 8、根据权利要求7所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在AllGather-GEMM场景下，远端输入分片按照行块优先的顺序进行拉取，并将局部复制屏障并入通信驱动流水线，使远端输入分片的拉取、细粒度可消费就绪状态的发布以及远端消费路径的触发按统一时序推进；同时，为数据复制路径分配独立的计算资源配额，以降低数据复制路径与本地快速路径之间的资源争用。

## 权利要求9（原稿编号重复的第二个8）
> 涉及批注：批注8：主行维、列维是专业术语吗/批注9：与权1步骤2什么关系/编号重复修复

**原稿：**

> 8、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在GEMM-ReduceScatter和GEMM-AllReduce场景下，将局部部分结果沿主行维划分为多个行块工作单元、沿列维划分为多个列带工作单元，并由所述行块工作单元与列带工作单元组合形成面板工作单元；当同一面板工作单元对应的全部源节点贡献到齐后，发布面板工作单元的可消费就绪状态。

**修改稿：**

> 9、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述局部工作单元在GEMM-ReduceScatter和GEMM-AllReduce场景下按照如下方式划分并建立就绪状态：将各并行节点执行矩阵乘得到的局部部分结果沿行方向划分为多个行块工作单元、沿列方向划分为多个列带工作单元，并由所述行块工作单元与所述列带工作单元组合形成面板工作单元；记录同一面板工作单元对应的各源节点贡献的到达情况，当全部源节点的贡献到齐后，发布该面板工作单元的可消费就绪状态。

## 权利要求10（原稿编号9）
> 涉及批注：批注10：与权1步骤2什么关系/编号顺延

**原稿：**

> 9、根据权利要求8所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，在GEMM-ReduceScatter场景下，采用前沿优先、尾段跟进的两阶段推进方式，优先推进最早可触发局部归约的前沿面板工作单元，并在前沿面板工作单元足以支撑消费侧持续工作后继续推进剩余面板工作单元；同时，仅为当前活动窗口内的面板工作单元分配对称暂存窗口，并在面板工作单元完成局部归约后释放对应槽位。

**修改稿：**

> 10、根据权利要求9所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述关键路径优先的推进方式在GEMM-ReduceScatter场景下采用前沿优先、尾段跟进的两阶段推进方式实现：优先推进最早能够触发局部归约的前沿面板工作单元，并在前沿面板工作单元足以支撑消费侧持续工作后，继续推进剩余的面板工作单元；同时，仅为当前活动窗口内的面板工作单元分配对称暂存窗口，并在面板工作单元完成局部归约后释放对应槽位。

## 权利要求11（原稿编号10）
> 涉及批注：编号顺延

**原稿：**

> 10、根据权利要求9所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤3中，在GEMM-ReduceScatter场景下，通过逻辑行块索引到物理槽位的映射复用所述对称暂存窗口，并使本地源节点对应的局部部分结果直接写入非对称输出缓冲区作为归约初值，以减少对称暂存窗口占用。

**修改稿：**

> 11、根据权利要求10所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤3中，在GEMM-ReduceScatter场景下，通过逻辑行块索引到物理槽位的映射复用所述对称暂存窗口，并使本地源节点对应的局部部分结果直接写入非对称输出缓冲区作为归约初值，以减少对称暂存窗口的占用。

## 权利要求12（原稿编号11）
> 涉及批注：编号顺延；开头补"所述关键路径优先的推进方式在…场景下"

**原稿：**

> 11、根据权利要求8所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤3中，在GEMM-AllReduce场景下，生产侧以面板工作单元作为矩阵乘输出的基本生产单元，并采用前沿优先、尾段跟进的两阶段推进方式优先生成最早可触发全局归约的前沿面板工作单元；消费侧将所述面板工作单元送入活动窗口内的归约槽位，并在对应面板工作单元所需源节点贡献到齐后立即触发窗口化局部全归约。

**修改稿：**

> 12、根据权利要求9所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤2中，所述关键路径优先的推进方式在GEMM-AllReduce场景下为：生产侧以面板工作单元作为矩阵乘输出的基本生产单元，采用前沿优先、尾段跟进的两阶段推进方式，优先生成最早能够触发全局归约的前沿面板工作单元；步骤3中，消费侧将所述面板工作单元送入活动窗口内的归约槽位，并在对应面板工作单元所需的全部源节点贡献到齐后，立即触发窗口化局部全归约。

## 权利要求13（原稿编号12）
> 涉及批注：编号顺延；引用对象由权11改为权12

**原稿：**

> 12、根据权利要求11所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤3中，在GEMM-AllReduce场景下，仅为当前活动窗口内的归约任务分配有限数量的暂存窗口槽位，所述暂存窗口槽位作为面板工作单元或列带分片的临时聚合位置，在相应归约完成后立即释放并循环复用。

**修改稿：**

> 13、根据权利要求12所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤3中，在GEMM-AllReduce场景下，仅为当前活动窗口内的归约任务分配有限数量的暂存窗口槽位，所述暂存窗口槽位作为面板工作单元或列带工作单元的临时聚合位置，在相应归约完成后立即释放并循环复用。

## 权利要求14（原稿编号13）
> 涉及批注：编号顺延

**原稿：**

> 13、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤4中，对目标局部工作单元完成矩阵乘消费、局部归约或全局归约后，提交对应结果并回收其占用的暂存窗口槽位，以供后续局部工作单元复用。

**修改稿：**

> 14、根据权利要求1所述的基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，其特征在于，步骤4中，对目标局部工作单元完成矩阵乘消费、局部归约或全局归约后，提交对应结果并回收其占用的暂存窗口槽位，以供后续局部工作单元复用。

---
# 二、说明书

## 2.1 具体实施方式之前：逐段修改（共9段，其余段落未动）

### 段落[3]·技术领域
> 涉及批注：批注4呼应：算子补中文译名

**原稿：**
> 本发明涉及分布式并行计算、GPU 高性能计算和张量并行推理加速技术领域，尤其涉及面向 AllGather-GEMM、GEMM-ReduceScatter 和 GEMM-AllReduce 等分布式矩阵乘相关算子的通信计算重叠加速及结构自适应配置方法，具体指一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法。

**修改稿：**
> 本发明涉及分布式并行计算、GPU高性能计算和张量并行推理加速技术领域，尤其涉及面向AllGather-GEMM（全收集-通用矩阵乘）、GEMM-ReduceScatter（通用矩阵乘-归约分散）和GEMM-AllReduce（通用矩阵乘-全归约）等分布式矩阵乘相关算子的通信计算重叠加速及结构自适应配置方法，具体指一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法。

### 段落[15]·发明内容·核心技术方案
> 涉及批注：呼应批注1：去掉"输入数据分片或局部部分结果"的二选一

**原稿：**
> 为实现上述发明目的，本发明采用的核心技术方案为：一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，通过对输入数据分片或局部部分结果进行细粒度划分并建立可独立发布的可消费就绪状态，结合关键路径优先的分岔调度方式、活动窗口受限的暂存槽位复用机制以及约束感知的目标执行配置参数确定过程，实现面向 AllGather-GEMM、GEMM-ReduceScatter 和 GEMM-AllReduce 三类算子的统一通信计算重叠加速，整个方法在不改变分布式矩阵乘相关算子原有数学语义和归约语义的基础上实施，具体包括以下四个核心步骤：

**修改稿：**
> 为实现上述发明目的，本发明采用的核心技术方案为：一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，通过对生产侧提供的待消费数据进行细粒度划分并建立可独立发布的可消费就绪状态，结合关键路径优先的分岔调度方式、活动窗口受限的暂存槽位复用机制以及约束感知的目标执行配置参数确定过程，实现面向AllGather-GEMM、GEMM-ReduceScatter和GEMM-AllReduce三类算子的统一通信计算重叠加速，整个方法在不改变分布式矩阵乘相关算子原有数学语义和归约语义的基础上实施，具体包括以下四个核心步骤：

### 段落[16]·发明内容·第一步
> 涉及批注：批注0：补生产侧/消费侧定义

**原稿：**
> 第一步，获取待执行的分布式矩阵乘相关算子的类型、矩阵问题形状、并行规模及硬件资源约束，建立对应的生产侧与消费侧依赖模型，并确定目标执行配置参数。

**修改稿：**
> 第一步，获取待执行的分布式矩阵乘相关算子的类型、矩阵问题形状、并行规模及硬件资源约束，建立对应的生产侧与消费侧依赖模型；其中，生产侧为提供数据的一方，包括在并行节点间传输数据的通信过程和生成局部部分结果的矩阵乘计算过程，消费侧为使用所述数据的一方，包括依赖所述数据执行的矩阵乘消费过程和归约过程；再根据所述依赖模型确定目标执行配置参数。

### 段落[17]·发明内容·第二步
> 涉及批注：批注1：数据表述按场景明确

**原稿：**
> 第二步，根据所述生产侧与消费侧依赖模型，将输入数据分片或局部部分结果划分为多个局部工作单元，为各局部工作单元建立细粒度可消费就绪状态，并采用关键路径优先的分岔调度方式组织计算与通信推进顺序。

**修改稿：**
> 第二步，根据所述生产侧与消费侧依赖模型，将生产侧提供的待消费数据（在AllGather-GEMM场景下为输入数据分片，在GEMM-ReduceScatter和GEMM-AllReduce场景下为局部部分结果）划分为多个局部工作单元，为各局部工作单元建立细粒度可消费就绪状态，并采用关键路径优先的推进方式确定通信与计算的推进顺序。

### 段落[18]·发明内容·第三步
> 涉及批注：补活动窗口定义，与权1一致

**原稿：**
> 第三步，仅为当前活动窗口内的局部工作单元分配有限数量、可循环复用的暂存窗口槽位，并依据所述目标执行配置参数执行局部工作单元的通信计算重叠。

**修改稿：**
> 第三步，仅为当前活动窗口内的局部工作单元分配有限数量、可循环复用的暂存窗口槽位，所述活动窗口为当前正在执行通信与计算重叠的局部工作单元的集合，并依据所述目标执行配置参数执行局部工作单元的通信计算重叠。

### 段落[21]·发明内容·优选1
> 涉及批注：批注4：算子补中文译名；collective→集合通信

**原稿：**
> 作为优选，所述分布式矩阵乘相关算子包括 AllGather-GEMM、GEMM-ReduceScatter 和 GEMM-AllReduce；其中，AllGather-GEMM 的消费依赖为输入分片聚合后再执行矩阵乘消费，GEMM-ReduceScatter 的消费依赖为各源节点的局部部分结果到齐后执行局部归约并保留目标输出段，GEMM-AllReduce 的消费依赖为各源节点的局部部分结果到齐后在各并行节点上执行全局归约。通过对三类算子依赖模式进行统一建模，可将不同 collective 语义下的通信计算重叠过程纳入同一执行框架。

**修改稿：**
> 作为优选，所述分布式矩阵乘相关算子包括AllGather-GEMM（全收集-通用矩阵乘）、GEMM-ReduceScatter（通用矩阵乘-归约分散）和GEMM-AllReduce（通用矩阵乘-全归约）；其中，AllGather-GEMM的消费依赖为输入分片聚合后再执行矩阵乘消费，GEMM-ReduceScatter的消费依赖为各源节点的局部部分结果到齐后执行局部归约并保留目标输出段，GEMM-AllReduce的消费依赖为各源节点的局部部分结果到齐后在各并行节点上执行全局归约。通过对三类算子依赖模式进行统一建模，可将不同集合通信语义下的通信计算重叠过程纳入同一执行框架。

### 段落[23]·发明内容·优选3
> 涉及批注：行维→行方向

**原稿：**
> 作为优选，在 AllGather-GEMM 场景下，将每个远端输入分片沿行维划分为多个行块工作单元，并在各行块工作单元完成复制后分别发布细粒度可消费就绪状态，以使消费侧在其所依赖的行块工作单元到达后触发对应输出块的矩阵乘计算。与整体分片到齐后再统一消费的方式相比，该方式能够提前暴露局部可消费边界，缩短远端输入等待造成的关键路径时延。

**修改稿：**
> 作为优选，在AllGather-GEMM场景下，将每个远端输入分片沿行方向划分为多个行块工作单元，并在各行块工作单元完成复制后分别发布细粒度可消费就绪状态，以使消费侧在其所依赖的行块工作单元到达后即触发对应输出块的矩阵乘计算。与整体分片到齐后再统一消费的方式相比，该方式能够提前暴露局部可消费边界，缩短远端输入等待造成的关键路径时延。

### 段落[27]·发明内容·优选6
> 涉及批注：批注8：主行维/列维→行方向/列方向

**原稿：**
> 作为优选，在 GEMM-ReduceScatter 和 GEMM-AllReduce 场景下，将局部部分结果沿主行维划分为多个行块工作单元、沿列维划分为多个列带工作单元，并由所述行块工作单元与列带工作单元组合形成面板工作单元；当同一面板工作单元对应的全部源节点贡献到齐后，发布所述面板工作单元的可消费就绪状态。采用面板工作单元作为统一的局部可消费粒度，能够兼顾局部行列相关性和后续归约组织效率。

**修改稿：**
> 作为优选，在GEMM-ReduceScatter和GEMM-AllReduce场景下，将局部部分结果沿行方向划分为多个行块工作单元、沿列方向划分为多个列带工作单元，并由所述行块工作单元与列带工作单元组合形成面板工作单元；当同一面板工作单元对应的全部源节点贡献到齐后，发布所述面板工作单元的可消费就绪状态。采用面板工作单元作为统一的局部可消费粒度，能够兼顾局部行列相关性和后续归约组织效率。

### 段落[34]·发明内容·有益效果1
> 涉及批注：呼应批注1：去掉"或"

**原稿：**
> 本发明通过对输入数据分片或局部部分结果进行细粒度划分，并为局部工作单元建立可独立发布的可消费就绪状态，使消费侧能够在局部依赖满足后尽早启动，有效缩短关键路径，解决了现有技术中局部工作单元可消费边界暴露过晚的问题。

**修改稿：**
> 本发明通过对生产侧提供的输入数据分片、局部部分结果等待消费数据进行细粒度划分，并为局部工作单元建立可独立发布的可消费就绪状态，使消费侧能够在局部依赖满足后尽早启动，有效缩短关键路径，解决了现有技术中局部工作单元可消费边界暴露过晚的问题。

## 2.2 具体实施方式：重构为3个实施例

**批注（实施例1处）：我们这个技术方案应该是3个平行的技术方案，所以需要把AllGather-GEMM、GEMM-ReduceScatter 和 GEMM-AllReduce拆分成3个实施例**

原段落去向表：

| 原稿段落 | 去向 | 处理方式 |
|---|---|---|
| [49] 引导语 | 保留 | 原样 |
| [50] 实施例1 | 实施例1标题 | 保留 |
| [51] 一种…包括如下步骤 | 三个实施例引言 | 改写为3段场景引言 |
| [52] 步骤1标题 | 实施例1/2/3步骤1标题 | 各实施例独立小标题 |
| [53] 依赖模型（混三场景） | 实施例1步骤1 / 实施例2步骤1 / 实施例3步骤1 | 拆分改写：AG句留实施例1并补生产侧/消费侧定义；RS、AR句分别改写为实施例2、3的依赖模型段 |
| [54] 配置参数（含4公式） | 实施例1步骤1 | **原样移入** |
| [55] 步骤2标题 | 实施例1/2/3步骤2标题 | 各实施例独立小标题 |
| [56] AG行块就绪（含5公式） | 实施例1步骤2 | 重写：行维→行方向；补"置位就绪标志""无需等待整片到达"；组织为→划入；补逐块触发语义。公式原样保留 |
| [57] 面板定义（RS+AR，含7公式） | 实施例2步骤2；实施例3步骤2 | 重写为RS口径（补"记录到达情况"）；实施例3复用其中4个公式简写。**补全V_j公式缺失的右方括号（原稿笔误）** |
| [58] 步骤3标题 | 实施例1/2/3步骤3标题 | 各实施例独立小标题 |
| [59] RS前沿推进+槽位（含8公式） | 实施例2步骤3 | **原样移入** |
| [60] AR生产+窗口归约（含3公式） | 实施例3步骤2末段 + 步骤3 | 拆分：首句（生产侧两阶段生产）→步骤2；其余（窗口归约，公式原样）→步骤3 |
| [61] 步骤4标题 | 实施例1/2/3步骤4标题 | 各实施例独立小标题 |
| [62] 触发消费（混三场景，含4公式） | 实施例1步骤4 / 实施例2步骤4 / 实施例3步骤4 | 拆分改写：AG句（公式Ready_ag、i）→实施例1；RS句（公式Ready_panel、(i,j)）→实施例2；AR复用同组公式→实施例3 |
| [63] 结尾段 | 结尾段 | 改写：点明三个实施例为三个平行技术方案 |

**全新增段落（原稿没有对应段）：**

1. 三个实施例的引言段（如"本实施例给出所述方法在AllGather-GEMM（全收集-通用矩阵乘）场景下的一种实施方式，如图1和图2所示，包括如下步骤："）
2. 实施例1步骤3（活动窗口受限的通信计算重叠执行）——内容取自原发明内容优选段[26]与原权8，具体化为执行步骤：接收缓冲槽位、行块优先拉取、统一时序、独立计算资源配额、写回区域互不重叠
3. 实施例2步骤1两段（RS依赖模型 + "配置参数确定与实施例1一致"并列带数量参数）
4. 实施例3步骤1（AR依赖模型 + 配置参数同实施例1）

以下给出被改写段落的逐段新旧对照（原样移入的[54][59]不再列出）：

### 原稿段落[51]·[51] 实施例引言

**原稿：**
> 一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，包括如下步骤：

**修改稿中的对应新段落：**

### 原稿段落[53]·[53] 依赖模型

**原稿：**
> 下面结合图1对本发明的统一依赖模型进行说明。本实施例以并行规模⟨P⟩、全局矩阵形状⟨M x N x K⟩及硬件资源约束作为输入，并将单卡局部行规模与局部K维规模分别记为 ⟨M_rank=M/P⟩ 和 ⟨K_rank=K/P⟩ ；对于按列切分权重的AllGather-GEMM，局部列分片记为 ⟨N_rank=N/P⟩ 。在 AllGather-GEMM 场景中，生产侧对应远端输入分片的拉取与本地输入分片的准备，消费侧对应依赖所述输入分片的局部矩阵乘计算；在 GEMM-ReduceScatter 与 GEMM-AllReduce 场景中，生产侧对应局部矩阵乘生成局部部分结果，消费侧对应局部归约或全局归约的触发与提交。借助该统一建模方式，可将三类算子的通信计算重叠过程纳入同一执行框架。

**修改稿中的对应新段落：**

### 原稿段落[56]·[56] AG行块就绪

**原稿：**
> 结合图2，在 AllGather-GEMM 场景下，将每个远端输入分片沿行维划分为多个行块工作单元，第⟨i⟩个行块的覆盖区间记为 ⟨U_i=(iR_ag,min((i+1)R_ag,M_rank))⟩ ，其中 ⟨R_ag⟩ 为 AllGather 路径的行块粒度。当某一行块复制完成后，即发布对应细粒度可消费就绪状态⟨ Ready_ag(i)=1⟩ 。与此对应，仅依赖本地输入分片的计算组织为本地快速路径，依赖远端输入分片的计算组织为远端消费路径；本地快速路径无需等待远端输入分片到达即可先行执行，远端消费路径则依据 ⟨Ready_ag(i)⟩ 逐块触发。优选地，远端输入分片按照行块优先顺序拉取，并将局部复制屏障并入通信驱动流水线，使远端输入分片拉取、就绪状态发布以及远端消费路径触发按统一时序推进。

**修改稿中的对应新段落：**

### 原稿段落[57]·[57] 面板定义

**原稿：**
> 结合图1、图3和图4，在 GEMM-ReduceScatter 和 GEMM-AllReduce 场景下，将局部部分结果沿主行维划分为多个行块工作单元，并沿列维组织为多个列带工作单元。若列带数量为⟨B⟩，则第 ⟨j⟩ 个列带可记 ⟨V_j=[j*ceil(N/B),min((j+1)*ceil(N/B),N)⟩ ，由行块工作单元⟨U_i⟩与列带工作单元⟨V_j⟩组合形成面板工作单元 ⟨Panel(i,j)=U_i x V_j⟩ 。当同一面板工作单元对应的全部源节点贡献到齐后，发布该面板工作单元的可消费就绪状态   ⟨Ready_panel(i,j)=1⟩ ，从而将局部归约或全局归约的触发粒度由整体输出段下沉到面板级别。

**修改稿中的对应新段落：**

### 原稿段落[60]·[60] AR生产与窗口归约

**原稿：**
> 结合图4，在 GEMM-AllReduce 场景下，生产侧以面板工作单元作为矩阵乘输出的基本生产单元，并同样采用前沿优先、尾段跟进的两阶段推进方式优先生成最早可触发全局归约的前沿面板；消费侧将所述面板工作单元送入活动窗口内的归约槽位，在对应面板所需源节点贡献到齐后立即触发窗口化局部全归约。优选地，活动窗口的暂存行容量满足 ⟨Q=W*B*P_local*R⟩ ，其中 ⟨P_local⟩ 表示单节点参与通信的本地设备数量，⟨R⟩ 表示当前面板的行块粒度。由此仅为当前活动窗口中的归约任务分配有限数量的槽位资源，所述槽位作为面板或带状分片的临时聚合位置，并在相应归约完成后立即释放并循环复用，从而形成图4所示的“面板生产、窗口化归约、槽位回收”连续流水线。

**修改稿中的对应新段落：**

### 原稿段落[62]·[62] 触发消费

**原稿：**
> 在目标局部工作单元满足消费依赖后，触发对目标局部工作单元的矩阵乘消费、局部归约或全局归约，并在对应结果提交后回收其占用的暂存窗口槽位，以供后续局部工作单元复用。对于 AllGather-GEMM，可在⟨Ready_ag(i)=1⟩时触发第⟨i⟩个行块的远端消费；对于 GEMM-ReduceScatter 与 GEMM-AllReduce，可在⟨Ready_panel(i,j)=1⟩时触发面板工作单元⟨(i,j)⟩的局部归约或全局归约。结合图2至图4，上述过程共同形成“局部工作单元到达并发布就绪状态、关键路径优先推进、活动窗口内暂存执行、完成消费后释放槽位”的闭环流水线。

**修改稿中的对应新段落：**

### 原稿段落[63]·[63] 结尾

**原稿：**
> 本领域技术人员可以理解，在不脱离本发明精神和保护范围的前提下，上述步骤中的局部工作单元划分方式、前沿推进策略、活动窗口大小、暂存窗口布局方式、列带组织方式以及配置参数确定细节均可根据具体硬件环境和问题规模进行调整，上述变化和改进均应落入本发明的保护范围内。本发明的保护范围以所附权利要求书及其等效技术方案为准。

**修改稿中的对应新段落：**

> 本实施例给出所述方法在AllGather-GEMM（全收集-通用矩阵乘）场景下的一种实施方式，如图1和图2所示，包括如下步骤：

> 本实施例给出所述方法在GEMM-ReduceScatter（通用矩阵乘-归约分散）场景下的一种实施方式，如图1和图3所示，包括如下步骤：

> 本实施例给出所述方法在GEMM-AllReduce（通用矩阵乘-全归约）场景下的一种实施方式，如图1和图4所示，包括如下步骤：

> 下面结合图1对本实施例的依赖模型进行说明。本实施例以并行规模⟨P⟩、全局矩阵形状⟨M x N x K⟩及硬件资源约束作为输入，并将单卡局部行规模与局部K维规模分别记为⟨M_rank=M/P⟩和⟨K_rank=K/P⟩；对于按列切分权重的AllGather-GEMM，局部列分片记为⟨N_rank=N/P⟩。在AllGather-GEMM场景中，生产侧对应远端输入分片的拉取与本地输入分片的准备，即通过节点间通信产生可供消费的输入数据分片的过程；消费侧对应依赖所述输入数据分片的局部矩阵乘计算，即使用所述输入数据分片执行矩阵乘并输出计算结果的过程。

> 结合图2，在AllGather-GEMM场景下，将每个远端输入分片沿行方向划分为多个行块工作单元，第⟨i⟩个行块的覆盖区间记为⟨U_i=(iR_ag,min((i+1)R_ag,M_rank))⟩，其中⟨R_ag⟩为AllGather路径的行块粒度。当某一行块对应的远端数据复制完成后，即以置位就绪标志的方式发布该行块工作单元的细粒度可消费就绪状态⟨ Ready_ag(i)=1⟩，表示该行块工作单元已可被消费侧单独消费，而无需等待整个远端输入分片全部到达。与此对应，仅依赖本地输入分片的计算划入本地快速路径，依赖远端输入分片的计算划入远端消费路径；本地快速路径无需等待远端输入分片到达即可先行执行，远端消费路径则依据⟨Ready_ag(i)⟩逐块触发，即每当一个行块工作单元的就绪状态发布后，立即触发该行块工作单元所对应输出块的矩阵乘计算。优选地，远端输入分片按照行块优先顺序拉取，并将局部复制屏障并入通信驱动流水线，使远端输入分片拉取、就绪状态发布以及远端消费路径触发按统一时序推进。

> 结合图1和图3，在GEMM-ReduceScatter场景下，将各并行节点执行局部矩阵乘得到的局部部分结果沿行方向划分为多个行块工作单元，并沿列方向组织为多个列带工作单元。若列带数量为⟨B⟩，则第⟨j⟩个列带可记⟨V_j=[j*ceil(N/B), min((j+1)*ceil(N/B), N)]⟩，由行块工作单元⟨U_i⟩与列带工作单元⟨V_j⟩组合形成面板工作单元⟨Panel(i,j)=U_i x V_j⟩。执行时，记录同一面板工作单元对应的各源节点贡献的到达情况，当全部源节点的贡献到齐后，发布该面板工作单元的可消费就绪状态⟨Ready_panel(i,j)=1⟩，从而将局部归约的触发粒度由整体输出段下沉到面板级别。

> 与实施例2类似，结合图1和图4，本实施例将各并行节点执行局部矩阵乘得到的局部部分结果沿行方向划分为多个行块工作单元，并沿列方向组织为多个列带工作单元，由行块工作单元⟨U_i⟩与列带工作单元⟨V_j⟩组合形成面板工作单元⟨Panel(i,j)=U_i x V_j⟩；当同一面板工作单元对应的全部源节点的贡献到齐后，发布该面板工作单元的可消费就绪状态⟨Ready_panel(i,j)=1⟩。

> 结合图4，在GEMM-AllReduce场景下，生产侧以面板工作单元作为矩阵乘输出的基本生产单元，并采用前沿优先、尾段跟进的两阶段推进方式，优先生成最早可触发全局归约的前沿面板工作单元。

> 消费侧将所述面板工作单元送入活动窗口内的归约槽位，在对应面板工作单元所需源节点贡献到齐后立即触发窗口化局部全归约。优选地，活动窗口的暂存行容量满足⟨Q=W*B*P_local*R⟩，其中⟨P_local⟩表示单节点参与通信的本地设备数量，⟨R⟩表示当前面板的行块粒度。由此，仅为当前活动窗口中的归约任务分配有限数量的槽位资源，所述槽位作为面板工作单元或带状分片的临时聚合位置，并在相应归约完成后立即释放并循环复用，从而形成图4所示的“面板生产、窗口化归约、槽位回收”连续流水线。

> 在目标行块工作单元满足消费依赖后，触发对该行块工作单元的矩阵乘消费：即当⟨Ready_ag(i)=1⟩时触发第⟨i⟩个行块的远端消费，对聚合后的输入数据分片执行矩阵乘计算，并在对应输出结果提交后回收其占用的暂存槽位，以供后续行块工作单元复用。结合图2，上述过程形成“输入分片到达并发布就绪状态、本地快速路径与远端消费路径并行推进、活动窗口内暂存执行、完成消费后释放槽位”的闭环流水线。

> 在目标面板工作单元满足消费依赖后，触发对该面板工作单元的局部归约：即当⟨Ready_panel(i,j)=1⟩时触发面板工作单元⟨(i,j)⟩的局部归约，保留对应的目标输出段，并在归约结果提交后回收其占用的暂存窗口槽位，以供后续面板工作单元复用。结合图3，上述过程形成“面板贡献到齐并发布就绪状态、前沿优先推进、活动窗口内暂存归约、完成归约后释放槽位”的闭环流水线。

> 在目标面板工作单元满足消费依赖后，触发对该面板工作单元的全局归约：即当⟨Ready_panel(i,j)=1⟩时触发面板工作单元⟨(i,j)⟩的全局归约，使各并行节点均得到一致的归约结果，并在归约结果提交后回收其占用的暂存窗口槽位，以供后续面板工作单元复用。结合图2至图4，实施例1至实施例3共同形成“局部工作单元到达并发布就绪状态、关键路径优先推进、活动窗口内暂存执行、完成消费后释放槽位”的闭环流水线。

> 以上通过实施例1至实施例3分别给出了本发明在AllGather-GEMM、GEMM-ReduceScatter和GEMM-AllReduce三类算子场景下的具体实施方式，三个实施例对应三个平行的技术方案。本领域技术人员可以理解，在不脱离本发明精神和保护范围的前提下，上述各实施例中的局部工作单元划分方式、前沿推进策略、活动窗口大小、暂存窗口布局方式、列带组织方式以及配置参数确定细节均可根据具体硬件环境和问题规模进行调整，上述变化和改进均应落入本发明的保护范围内。本发明的保护范围以所附权利要求书及其等效技术方案为准。

---
# 三、说明书摘要（全文对照）

**原稿（411字，超出300字上限）：**

> 本发明公开了一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，属于分布式并行计算与GPU高性能计算技术领域。该方法面向AllGather-GEMM、GEMM-ReduceScatter和GEMM-AllReduce三类分布式矩阵乘相关算子，根据算子类型、矩阵问题形状、并行规模及硬件资源约束建立统一的生产侧与消费侧依赖模型，并确定局部切分粒度、前沿推进深度、活动窗口大小和槽位数量等目标执行配置参数；将远端输入分片或局部部分结果划分为行块工作单元或面板工作单元，为各工作单元发布细粒度可消费就绪状态，并采用前沿优先、尾段跟进的方式组织计算与通信重叠执行；执行过程中仅为活动窗口内工作单元分配有限数量且可循环复用的暂存槽位，在目标工作单元满足依赖后立即触发矩阵乘消费、局部归约或全局归约，并在完成后释放槽位。该方法能够更早暴露局部可消费边界，提升通信与计算重叠效率，降低暂存开销，并提高大规模并行场景下的执行稳定性。

**修改稿（300字）：**

> 本发明公开了一种基于细粒度就绪与活动窗口的分布式计算通信重叠加速方法，属于分布式并行计算与GPU高性能计算技术领域。面向AllGather-GEMM、GEMM-ReduceScatter和GEMM-AllReduce三类分布式矩阵乘算子，按矩阵形状、并行规模及硬件约束建立生产侧与消费侧依赖模型并确定目标执行配置参数；将生产侧数据划分为多个局部工作单元并发布细粒度可消费就绪状态，以关键路径优先方式重叠执行；仅为活动窗口内工作单元分配可复用暂存槽位，满足消费依赖后立即触发矩阵乘消费、局部归约或全局归约并释放槽位。本方法可更早暴露局部可消费边界，提升重叠效率，降低暂存开销，提高大规模并行下的稳定性。

删减内容："前沿优先、尾段跟进"提法、"行块工作单元或面板工作单元"具体划分、"远端输入""各源节点"等细节词；统一为"关键路径优先""局部工作单元"。新增："生产侧数据""关键路径优先"与权1术语对齐。

---
# 四、其他

1. **说明书附图**：未改动，原样复制。
2. **批注清除**：修改稿四份文档已剥离全部批注部件（原稿11+2条批注仅存在于原稿）。
3. **版式**：全文楷体小四、首行缩进2字符与原稿一致；章节标题加"与下段同页"防止孤悬页底。
4. **原稿笔误修复**：[57]段V_j区间公式补右方括号。