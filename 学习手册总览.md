# 📚 学习资料总索引（睡醒先看这个）

> 你睡后产出的 6 本新手册 + 原有 3 套面试笔记，全部在这里。
> 所有实验代码**都已在本机编译运行验证**（验证结果标注在各篇）。

## 🆕 六本新手册（本次产出）

| 文件夹 | 内容 | 篇数 | labs |
|--------|------|------|------|
| `cpp_handbook/` | **C++ 教科书**：C01 变量 → C09 并发 → C11 现代 C++ 速查 | 13 篇 | 10 个（全过） |
| `cuda_handbook/` | **CUDA 教科书**：G01 架构 → G07 wmma → G09 速查表 | 10 篇 | 6 个（全过） |
| `cpp_cuda_handbook/` | **C++×CUDA 结合部**：RAII 显存 → kernel 包装器 → mini 张量类毕业项目 | 7 篇 | 5 个（全过） |
| `cmake_handbook/` | **CMake**：最小工程 → CUDA 工程模板（LeetCUDA 同款）→ 速查 | 3 篇 | 2 个可跑工程 |
| `pybind11_handbook/` | **pybind11**：最小模块 → Torch 扩展/LeetCUDA 模式 | 2 篇 | 1 个（GPU 模块真实 import 成功）|
| `cutlass_handbook/` | **CUTLASS 4.6.1 实战**（用你本地 third_party/cutlass）：官方 GEMM 精读 → 自写 fp16 Tensor Core GEMM（17.7 TFLOPS✓）→ 三层 GemmShape → 布局/TensorRef → Split-K → **epilogue 定制（ReLU/GELU 融合实测）** | 8 篇 | 4 个（全验证）|

**每篇统一格式**：编号名字 → 作用详解 → 可编译示例 → 踩坑 → 【前置标注】（如【前置：C03 §2】表示不懂先回看）。
**每个文件夹开头有总览篇**（C00/G00/X00）：依赖图 + 学习顺序 + 编译命令。

## 📖 三套面试笔记（之前完成，本次也打磨过）

| 文件夹 | 内容 | 状态 |
|--------|------|------|
| `cpp_interview_notes/` | T1×16 + T2×14 + T3×8 = 38 题面试问答 + 代码 | 钩子全配人话版 |
| `cuda_interview_notes/` | 25 题 + LeetCUDA 绑定 + 4060 可学清单 | 钩子全配人话版 |
| `os_net_interview_notes/` | OS×7 + 网络×5 = 12 题 + 12 个实测 demo | 钩子全配人话版 |

## 🗺️ 建议使用顺序

1. **现在**：cpp_handbook 从 C01 读起（补地基），每晚 1-2 篇 + lab 跑一遍；
2. **C06 之后**：回到 cpp_interview_notes/T1 复习（会发现面试题变简单了）；
3. **C09 之后**：进 cuda_handbook G02 动手（4060 上全部可跑）；
4. **G06 学完**：重看你的 overlap 课题——G06 的双流模式就是论文的微观原型；
5. **找工作前**：cpp/cuda/os_net 三套面试笔记总复习（钩子自测模式）；
6. **读 LeetCUDA 源码前**：cpp_cuda_handbook X 系列 + cmake/pybind 两本手册。

## ✅ 验证记录

- cpp labs：C01-C09、C12 共 10 个，`g++ -std=c++17 -O2 -pthread` 全部编译通过并运行；
- cuda labs：G02-G07 共 6 个，`nvcc -arch=sm_89` 编译通过并运行（实测数字写在各 md）；
- cpp_cuda labs：X01-X05 共 5 个，编译通过并运行（X05 毕业项目输出"零泄漏 ✓"）；
- cmake labs：2 个工程构建+运行成功（含 CUDA 工程）；
- pybind lab：GPU 模块真实 import，`add_gpu([1,2,3], 10)` → `[11,12,13]`；
- 过程中抓到并修复 8 个真实 bug（越界、布局错位、宏名手误等），均已写成注释/警告框留在代码里。
