# C07 移动语义与值类别 【进阶】

> C++11 最重要的性能特性。理解它 = 理解"为什么现代 C++ 代码不再拷贝大对象"。

## §C07.1 左值与右值

**作用**：值类别决定"这个表达式能不能被搬空"。

```cpp
int a = 42;
a;              // 左值: 有名字、有固定地址, 表达式结束后还在
42;             // 右值(纯右值): 临时的、马上就要消失的——"42"这个字面量
a + 1;          // 右值: 计算出来的临时结果
std::string("hi");   // 右值: 临时对象
```

判断法：**能对它取地址（&x 合法）→ 左值；临时的、即将消亡的 → 右值**。

## §C07.2 移动构造 vs 拷贝构造

**作用**：拷贝 = 复制全部内容（O(n)）；移动 = 把源对象的内部指针"抢"过来（O(1)）。

```cpp
std::string s1 = "hello world, a long string";
std::string s2 = s1;              // 拷贝构造: 真的复制一遍内容
std::string s3 = std::move(s1);   // 移动构造: s3 接管 s1 的内部指针, s1 变空壳
// 移动后 s1 处于"有效但未定义"状态: 能赋新值, 别假设它还有旧内容
```

【前置：T1-09 std::move 本质（含 const 陷阱）、T1-10 触发时机、T2-18 RVO】

### §C07.2.1 `&&` 语法本身 + 两个重载的分流实验（实测）

**`&&` = 右值引用类型**。它存在的意义不是"引用一个右值"这个动作本身，而是**给编译器一个分流标签**：同名重载同时提供 `const T&`（拷贝版）和 `T&&`（移动版）时，编译器按传入对象的"死活"自动选路。

```cpp
#include <cstdio>
#include <utility>
struct Vec3 {};
void who(const Vec3&) { printf("const Vec3& (拷贝版: 只能小心抄)\n"); }
void who(Vec3&&)      { printf("Vec3&&     (移动版: 可合法掏空)\n"); }
int main() {
    Vec3 a;
    who(a);            // ① 左值   → const Vec3&
    who(Vec3{});       // ② 临时值 → Vec3&&
    who(std::move(a)); // ③ 贴标签 → Vec3&&  (a 本身没死! 只是承诺不再用)
}
// 实测输出:
// ① const Vec3& (拷贝版...)
// ② Vec3&&     (移动版...)
// ③ Vec3&&     (移动版...)
```

**三个要点**（对照你 C05 的 Vec3 练习）：

1. **const T& 是万能备胎**：左右值都能绑。所以"没写移动构造时，临时对象也能构造"——走的是 const T& 的拷贝构造（你 main 里 `Vec3 v3 = v1 + v2;` 就是这么兜底的）。
2. **T&& 是专用快车道**：一旦存在，右值**优先**走它，性能自动升级（拷贝→偷指针）。
3. **std::move(a) 不杀 a**：它只是把 a 强转成右值（贴"临终"标签），a 还活着（处于"有效但未定义"状态），**不许再用它的内容**。

**人话版**：`&&` 是"**只接将死之人**"的专用通道。有这条通道，编译器才敢见人下菜碟——常驻居民（左值）走"慢慢抄"，临终患者（右值）走"器官捐献"（掏空指针）。

**记忆钩子**："const& 通吃，&& 优先接临时；move 只贴标签不动手。"

**自测**：① `who(std::move(a))` 之后再调 `who(a)` 走哪个版本？（还是 const Vec3&——a 作为变量仍是左值，标签只在那一次表达式有效）② 只删掉 `void who(const Vec3&)`，`who(a)` 还能编过吗？（不能——Vec3&& 绑不了左值；这正是"专用通道"的含义）

## §C07.3 移动构造函数怎么写

**作用**：自己的类要有"可搬"能力，就实现移动构造。

```cpp
class Buffer {
public:
    explicit Buffer(size_t n) : size_(n), data_(new char[n]) {}
    ~Buffer() { delete[] data_; }

    Buffer(const Buffer& o)                       // 拷贝: 深拷贝
        : size_(o.size_), data_(new char[o.size_]) { std::copy(o.data_, o.data_+o.size_, data_); }

    Buffer(Buffer&& o) noexcept                   // 移动: 抢指针! 必须标 noexcept
        : size_(o.size_), data_(o.data_) {        // (vector 扩容只敢用 noexcept 的移动)
        o.data_ = nullptr; o.size_ = 0;           // 源对象置空, 防止双重释放
    }
    Buffer& operator=(Buffer&& o) noexcept {      // 移动赋值: 同理
        if (this != &o) { delete[] data_; data_ = o.data_; size_ = o.size_;
                          o.data_ = nullptr; o.size_ = 0; }
        return *this;
    }
private:
    size_t size_; char* data_;
};
```

【坑】移动构造/移动赋值**必须标 noexcept**——否则 vector 扩容不敢用移动只敢用拷贝
（强异常安全要求，前置：T1-10）。

## §C07.4 std::move 的真相

**作用**：`std::move(x)` **什么都不搬**，只是一个到右值引用的强转 `static_cast<T&&>(x)`——
给编译器一个"允许掠夺"的标记。真正干活的是随后的移动构造/移动赋值。

```cpp
std::vector<int> v(1000000);
auto w = std::move(v);      // v 的百万元素"易主"给 w, 没有任何元素被复制
printf("%zu\n", v.size());  // 0 (变空壳)
```

【前置：T1-09】const 对象 move 静默退化成拷贝；返回局部变量**别写** return std::move（T2-18 RVO）。

### §C07.4.1 移动后源对象变成什么样？——掏"资源"，不掏"数字"（实测）

**新手最疑惑的一幕**：移动之后打印源对象，发现 name 空了、但数字还在——"不是说全掏空吗？"

**答案：移动偷的是"资源"（堆上的东西），数字本来就掏不空**。全字段实测：

```cpp
struct T {
    int num;                        // 数字成员: 4字节长在对象身上, 没法"偷"
    std::vector<float> data;        // 堆资源: 偷内部数组
    std::string name;               // 堆资源: 偷内部缓冲
    char* buffer;                   // 裸指针资源: 偷指针+置空
    T& operator=(T&& o) noexcept {
        num = o.num;                        // 数字: "移动"它=拷贝(它就在那, 拿不走)
        data = std::move(o.data);
        name = std::move(o.name);
        buffer = o.buffer; o.buffer = nullptr;
        return *this;
    }
};
// b = std::move(a) 之后打印 a:
// 移动后a 数字num=42 | data.size()=0 | name="" | buffer=nullptr(被掏)
// 移动后b 数字num=42 | data.size()=3 | name="hello" | buffer=还在
```

**三条结论**：

1. **数字成员（int/float/指针本身等）移动后留在源头完全正常**——它们就几字节、长在对象身上，"偷"没有意义，移动它们就是拷贝。
2. **被掏空的是资源**：vector 的元素、string 的字符、你的 buffer——这些住在**堆上**、靠指针够着的东西才是值得偷的（偷个指针 8 字节 vs 复制堆上 1MB）。
3. **标准的态度："valid but unspecified"（合法但内容不明）**——移动后的对象只保证能析构、能赋新值；数字留旧值还是垃圾值都合规。所以规矩是：**移动后别再读它的内容**（读了也不是崩，是读到不确定的东西）。

**人话版**：搬家公司（移动）搬走的是**家具**（堆资源——值钱、搬走才省事），**墙皮**（数字成员——就刷在墙上那点）它不敲也不带走，留在旧房子里你也用不着管。

**记忆钩子**："**掏资源，留数字；移动后，不再读。**"

**自测**：你的 Vec3 移动后 `v3.print()` 显示 x/y/z 还在——给 `print()` 加打印 `_data.size()`，验证 v3 的 `_data` 其实也被掏空了（应显示 0）。

### §C07.4.2 三种编译结果亲测：从"拷贝"到"移动"再到"零构造"

对着 `Vec3 v3 = v1 + v2;`（operator+ 返回临时对象）实测三连：

| 编译方式 | 实测结果 | 原因 |
|---|---|---|
| **C++17（现代默认）** | **零次**额外构造 | **强制拷贝消除**：临时对象直接在 v3 的内存位置出生（语言规则，`-fno-elide-constructors` 都关不掉） |
| C++14 + `-fno-elide-constructors` | **移动构造 ×2** | 不消除时：临时 → 移动进返回槽 → 移动进 v3。类没写移动构造才会退化成拷贝 |
| C++14 常规优化 | 零次 | 编译器主动消除（优化，非强制） |

**结论**：现代 C++ 里 `v3 = v1 + v2` 这类"接临时值"的代码**什么构造都不调**——比移动还快。这也是"**return 局部变量别写 std::move**"的原因：你手动 move 反而**关掉**了编译器的零拷贝消除，画蛇添足变两次移动（T2-18）。

**记忆钩子**："消除 > 移动 > 拷贝；return 里的 std::move 是帮倒忙。"

## §C07.5 移动语义的工程价值

1. **容器与算法**：vector 存大对象时，元素可移动 = 扩容/排序快几倍；
2. **所有权转移**：`unique_ptr` 全靠移动语义（禁拷贝、送移动，前置：C06.2）；
3. **生产者-消费者**：队列里传大 buffer 用 move，避免整块 memcpy；
4. **CUDA 语境**：host 端预填充一个大 vector 后 `std::move` 交给管理类，避免 G 级数据二次拷贝
   （X05 的 mini 张量类会实际用到）。

## §C07.6 完美转发 std::forward（进阶）

**【头文件】`<utility>`**（std::move 的同一个家；不是 `<memory>`）。

**作用**：在"转发函数"里把参数**原封不动**传给下一层——原来是左值就还是左值，
原来是右值就还是右值。"完美"= 值类别不丢失。

### 问题：转发函数是个"降级中转站"

`make_unique` / `emplace_back` 这类工厂的通用形态是：**收一堆参数 → 转给真正的构造函数**。
麻烦在于一条铁律：**参数一进函数体就有了名字，有名字 = 左值**（§C07.2.1 学过）——
所以原来的右值（临时对象）经过转发函数一倒手，**降级成了左值**，内层永远选拷贝：

```cpp
#include <utility>
#include <string>
struct Widget {
    Widget(const std::string&) { printf("拷贝构造\n"); }   // 收左值
    Widget(std::string&&)      { printf("移动构造\n"); }   // 收右值
};

template <class... Args>
Widget make_bad(Args&&... args) {      // 万能引用(见下): 左右值都收
    return Widget(args...);            // ⚠️ args 有名字 → 永远是左值 → 内层永远拷贝!
}

template <class... Args>
Widget make_good(Args&&... args) {
    return Widget(std::forward<Args>(args)...);   // 保真: 原右值转回右值, 原左值保持左值
}

std::string s = "hello";
make_bad(s);                     // 拷贝 ✓ (本来就是左值, 没毛病)
make_bad(std::string("tmp"));    // 拷贝 ✗ 期望移动! 右值被中转站降级了
make_good(std::string("tmp"));   // 移动 ✓ forward 保住了它的右值身份   (实测输出)
```

### std::forward 是什么：**有条件的 std::move**

- `std::move(x)`：**无条件**转成右值（"反正我不要了"）
- `std::forward<T>(x)`：**查户口**——看模板推导时 T 记下的原始身份，原来是右值才转右值，
  原来是左值就不动（"如果它本来是要捐的，才当捐的处理"）
- 所以 forward **必须写模板参数** `std::forward<Args>(args)`——它要靠 Args 里记的"户口"判断

### 万能引用与引用折叠（`Args&&` 为什么左右通吃）

模板里的 `T&&` **不是**普通右值引用，是**万能引用**（universal reference）：推导规则——

| 传入 | T 推导成 | `T&&` 折叠成 |
|---|---|---|
| 左值 `string s` | `string&` | `string& &&` → **`string&`**（左值引用）|
| 右值 `string("tmp")` | `string` | **`string&&`**（右值引用）|

折叠口诀：**"有 & 则 &"**（两个引用类型叠一起，只要出现一个左值引用，结果就是左值引用）。
这就是 forward 能查户口的原理：**T 里记录着调用方传的是左还是右**。

### 实战落点（你全用过）

1. `std::make_unique<T>(args...)` 内部就是
   `unique_ptr<T>(new T(std::forward<Args>(args)...))`；
2. `vector::emplace_back(args...)` 同理——**C04 里"emplace_back 收零件就地构造"的机制就是这个**
   （零件原封不动传给元素的构造函数，右值零件还能保住移动）；
3. 你的手搓 `shared_ptr` 想升级成 `make_shared` 风格工厂，就用这招。

**人话版**：转发函数是海关中转站，所有包裹（参数）一进站就被贴上"本地件"（左值）标签；
forward 干的就是查原始运单（模板参数 T 里的记录），**该是加急件的恢复加急，普通件保持普通**。

**记忆钩子**："**move 无条件，forward 有条件；万能引用来者不拒，有名即左值，转发要查户口。**"

**自测**：① make_bad 里把 `Widget(args...)` 改成 `Widget(std::move(args)...)`，
传左值 s 会发生什么？（s 被当右值掏空——s 还要用，闯祸了。这正说明"无脑 move"不行，
才需要"查户口"的 forward）② `Args&&` 和 `Widget&&`（构造函数参数）谁是万能引用？
（前者：模板推导中；后者：类型定死的普通右值引用）

【前置：T3-36 引用折叠规则】这就是 `emplace_back` 能就地构造的机制（T1-12）。

## 本篇实验

`labs/C07_move.cpp`——用计数器证明 move 比 copy 快、noexcept 对 vector 扩容的影响、move 后源状态。
